# Healthy Bite

Одностраничный лендинг о здоровом питании с заготовками под дополнительные страницы (AI, блог, консультации) и черновиком бэкенда. Сейчас функционал преимущественно статический; большинство файлов-заглушек пустые, текст на главной частично повреждён (проблема с кодировкой).

## Содержание
- Возможности
- Структура проекта
- Фронтенд
- Бэкенд (статус/планы)
- Установка и запуск
- Переменные окружения
- Тесты
- Известные проблемы и TODO

## Возможности (текущее)
- Герой-блок с кнопкой перехода к AI-разделу.
- Видео-блок со слайдшоу (8 локальных MP4 в `frontend/assets/videos`), запуск по кнопке `#startVideos`.
- Раздел «About» с иллюстрацией.
- Плавный скролл навигации (см. `frontend/js/nav.js`).
- Лёгкая анимация появления секций (`fade-up`).

## Структура проекта
- `frontend/`
  - `index.html` — главная страница (единственная наполненная).
  - `about.html`, `ai.html`, `blog.html`, `consultation.html`, `contact.html` — пустые заглушки.
  - `css/` базовые стили (`base.css`, `layout.css`, `components.css`), стили страниц (`pages/home.css`, `pages/about.css`, остальные пусты), `responsive.css`, `themes.css` (пустые).
  - `js/` логика (`main.js`, `nav.js`), прочие файлы пусты (`ai.js`, `forms.js`, `lang.js`, `ui.js`).
  - `data/` (`ai-data.json`, `content.json`) — пустые.
  - `lang/` (`en.json`, `ru.json`, `am.json`) — пустые.
  - `assets/` иконки, изображения, большие видеофайлы.
- `backend/`
  - `app.py`, `config.py`, `.env` — пустые.
  - `api/` (`ai_routes.py`, `consult_routes.py`, `user_routes.py`) — пустые.
  - `models/user_model.py` — пустой.
  - `services/ai_core.py`, `services/auth.py` — пустые.
- Корень: `docker-compose.yml`, `requirements.txt`, `run.py`, `README.md` — пустые.

## Фронтенд
- Чистый HTML/CSS/JS без сборщиков.
- Шрифты: системный `Segoe UI`.
- Цветовая палитра: зелёные акценты (`#27ae60`, `#2ecc71`, `#1abc9c`), градиенты в hero/about.
- Скрипты:
  - `js/main.js`: слайдшоу видео (`#heroPlayer` — но в `index.html` элемент назван иначе: `#hero-video video`; нужен рефактор), старт по кнопке `#startVideos`, интервал 5 сек, базовая анимация `fade-in`.
  - `js/nav.js`: плавный скролл якорей.
  - Inline-скрипт в `index.html`: дублирует логику видео с небольшими отличиями и логами; использует IntersectionObserver для `fade-up`.
- Известные проблемы фронтенда:
  - Текст в `index.html` повреждён (кракозябры, вероятно UTF-8/Win-1251). Требуется восстановление русских строк.
  - Несогласованность селекторов: `main.js` ищет `#heroPlayer`, а в разметке `<section class="hero-video" id="hero-video"><video ...></video></section>`.
  - Дублирование логики видео: и в `main.js`, и inline в `index.html`; стоит оставить один источник.
  - Пустые страницы и файлы стилей/скриптов — нужно наполнить или удалить.
  - Большие видеофайлы (до ~48 МБ) — учесть при деплое/оптимизации.

## Бэкенд (статус/планы)
- Код отсутствует, но структура напоминает Flask/FastAPI-проект:
  - `app.py` — точка входа, нет содержимого.
  - `config.py` — ожидание конфигурации.
  - `api/*` — предполагаемые роуты для AI, консультаций, пользователей.
  - `services/ai_core.py`, `auth.py` — сервисы для AI и аутентификации.
  - `models/user_model.py` — модель пользователя (ORM не задан).
- `requirements.txt` пустой — зависимости не определены.
- `docker-compose.yml` пустой — контейнеризация не настроена.
- `.env` пустой — переменные окружения не заданы.
- Рекомендуемый старт для наполнения (пример для Flask + SQLAlchemy + OpenAI/LLM):
  - Добавить `Flask`, `Flask-Cors`, `pydantic`, `SQLAlchemy`/`PostgreSQL` (или SQLite), `python-dotenv`, клиент LLM по требованию.
  - В `app.py` создать приложение, подключить Blueprints из `api/`.
  - В `config.py` описать переменные (`DATABASE_URL`, `OPENAI_API_KEY` и т.п.).
  - В `models/user_model.py` описать модель и миграции (Alembic).
  - В `services/ai_core.py` инкапсулировать вызовы LLM.
  - В `services/auth.py` реализовать JWT/сессии.
  - Заполнить `requirements.txt` и добавить базовый `docker-compose.yml` (web + db).

## Установка и запуск (текущее)
Фронтенд:
1) `cd frontend`
2) Запустить статический сервер (любой). Пример:
   - Python: `python -m http.server 8000`
   - Или открыть `index.html` напрямую в браузере (видео и JS работают локально, без API).
3) Открыть `http://localhost:8000` (или файл) и нажать кнопку «Start videos» для запуска слайдшоу.

Бэкенд: не реализован. После добавления кода — стандартные шаги:
- Создать и активировать venv.
- Установить зависимости из `requirements.txt`.
- Настроить `.env`.
- Запустить `python app.py` или `flask run`/`uvicorn` — в зависимости от выбранного фреймворка.
- При использовании Docker — дописать `docker-compose.yml` и поднять сервисы `docker-compose up`.

## Переменные окружения
Пока отсутствуют. Минимальный набор, который понадобится после реализации бэкенда:
- `FLASK_ENV` / `APP_ENV`
- `PORT`
- `DATABASE_URL`
- Ключи для внешних API (LLM, почта и т.д.)

## Тесты
Тестов нет. После появления бэкенда стоит добавить:
- Unit-тесты сервисов (`services/`).
- Интеграционные тесты API (`api/`), например `pytest` + `httpx`.
- Линтер/форматтер (`flake8`, `black`, `isort`).

## Известные проблемы и TODO
- [ ] Восстановить текст в `frontend/index.html` (фикс кодировки, заменить кракозябры на исходный русский/англ текст).
- [ ] Убрать дублирование видео-логики: выбрать `main.js` или inline-скрипт, согласовать селекторы (`#heroPlayer` vs `#hero-video video`).
- [ ] Заполнить пустые страницы (`about.html`, `ai.html`, `blog.html`, `consultation.html`, `contact.html`) и связанные стили/скрипты.
- [ ] Наполнить `lang/*.json` и `data/*.json` для мультиязычности и контента.
- [ ] Добавить адаптивность в `css/responsive.css`.
- [ ] Инициализировать бэкенд: выбрать стек, описать зависимости в `requirements.txt`, реализовать роуты и модели.
- [ ] Настроить `docker-compose.yml` (web + db + прокси при необходимости).
- [ ] Добавить README в репозиторий и удалить пустые заглушки либо заполнить их содержимым.






















📘 Healthy Bite — Полный README-Summary проекта (Frontend)

Статичный мультистраничный сайт: главная, About, AI, Blog, Contact.
Сильный упор на чистую структуру, адаптив, i18n, плавные переходы и видео-слайдер.

1. Что уже сделано
✅ 1.1. Готовая структура фронтенда
frontend/
 ├── index.html
 ├── about.html
 ├── ai.html (заглушка)
 ├── blog.html (заглушка)
 ├── contact.html (заглушка)
 ├── consultation.html (заглушка)
 ├── css/
 │    ├── base.css
 │    ├── layout.css
 │    ├── components.css
 │    ├── pages/
 │    │     ├── home.css
 │    │     └── about.css
 ├── js/
 │    ├── main.js
 │    ├── nav.js
 │    ├── lang.js
 ├── assets/
 │    ├── videos/ (8 роликов)
 │    ├── images/
 │    └── icons/
 └── lang/
       ├── en.json
       ├── ru.json
       └── am.json

2. Главный функционал проекта
🎥 2.1. Видео-слайдер на главной (main.js)

Автосмена 8 mp4 файлов.

Плавный белый переход (overlay).

Автоматический бесконечный цикл.

Обработка ошибок (videoError).

Лёгкий зум для устранения чёрных полос.

🌐 2.2. Мультиязычность (lang.js)

Поддерживаемые языки: en, ru, am.

Определение языка по:

localStorage

браузеру

дефолт — английский

Перевод текста по data-i18n.

Важный момент: перевод НЕ трогает href ссылок — это исправлено.

Подсветка активной кнопки языка.

📱 2.3. Навигация (nav.js)

Бургер-меню на мобильных.

Плавный скролл по якорям.

Закрытие меню после выбора пункта.

На десктопе — выпадающая расширенная панель (header-panel) при наведении.

📄 2.4. About-страница полностью переработана
Секции:

Hero (картинка + CTA)

How Healthy Bite works — добавлено и стилизовано

Mission / Story

Principles

Audience

Team

CTA-блок

Гарантировано исправлено:

How it works ведёт в свой блок (#how-it-works)

Mission → #mission

Team → #team

Все якоря работают корректно с sticky-header (используем scroll-margin-top)

3. Исправления, которые уже внедрены
✔ Исправлена главная проблема:

How it works в панели не скроллил → обе ссылки вели на #mission.

Исправления:

Переписан about.html:

верная структура;

правильные id;

корректные ссылки внутри панели;

добавлен полноценный блок “How Healthy Bite works”.

В index.html:

панель теперь направляет на about.html#how-it-works.

В about.css:

добавлены оффсеты scroll-margin-top для корректной прокрутки.

Проверено, что lang.js не затирает href.

4. Что осталось пустым / черновым

(Чтобы в новом чате ты сразу понимал, куда ещё работать.)

ai.html — только шаблон (нет контента).

blog.html — пустой шаблон.

contact.html — простой шаблон.

consultation.html — черновой.

Нет backend-части, только статика.

Нет сборщика (Vite/Webpack) — сейчас raw HTML/CSS/JS.

5. Как запустить
🟢 Локально (любой метод)
Вариант 1 — VS Code Live Server

Открыть папку frontend/

Правый клик → Open with Live Server

Откроется http://127.0.0.1:5500/index.html

Вариант 2 — Python
cd frontend
python -m http.server 8000


Открыть:

http://localhost:8000/index.html

Вариант 3 — Node
npx serve frontend

6. Как дальше развивать проект
🔥 6.1. Добавить контент на страницы:

AI (описание фич)

Blog (список статей)

Contact (форма)

Consultation (форма записи)

Footer

🤖 6.2. Подключить backend (если потребуется)

Лёгкий Flask/FastAPI для обработки:

форм сообщений

консультаций

генерации AI-планов (если это цель продукта)

⚙️ 6.3. Улучшить i18n

Добавить переводчик для всех ещё не перенесённых текстов.

Добавить RTL-поддержку (если когда-то понадобится арабский/иврит).

📱 6.4. Полный адаптив для всех страниц

На About-странице всё ок.

На Blog/Contact — нужно дорабатывать.

🔍 6.5. SEO и OpenGraph

meta description

OG-картинки

микроданные

7. Технический стек проекта

HTML5

CSS3 (grid + responsive)

Vanilla JavaScript

i18n JSON словари

No frameworks

8. Главные моменты, чтобы помнить

Панель (header-panel) работает только на десктопах.

На мобильных она скрыта — используй бургер-меню.

Якоря должны иметь scroll-margin-top, иначе header их перекроет.

Видео-слайдер всегда должен иметь mp4 в assets/videos.

Переводы не должны содержать вложенных объектов в текстах типа:

"how": { "href": "#mission" }


Любые новые секции About должны добавляться только в about.html, а не в index.

9. Готов к переносу в новый чат

Если ты в новом чате напишешь просто:

Привет, продолжим HealthyBite, вот мой summary.

— я продолжу отсюда без пересборки контекста.












📘 Healthy Bite — Полный README-Summary проекта

(актуальный на момент последнего чата)

1. Стек и формат проекта

Статический сайт (без backend), структура:

HTML — мультистраничный: index, about, ai, blog, contact, consultation.

CSS — модульно: base.css, layout.css, components.css, + page-level стили.

JS — lang.js (i18n), nav.js (навигация + бургер), main.js (видео-слайдер).

Используем только чистые HTML/CSS/JS, без фреймворков.

2. Что уже сделано (важно)
2.1. Светлый общий стиль

Единая палитра: зелёный (#27ae60), белый (#fff), светло-мятный (#f9fff9).

Круглые карточки, мягкие тени, адаптивная сетка с grid и flex.

2.2. Полностью рабочая мультиязычность

Файлы: /lang/en.json, /ru.json, /am.json.

Работает:

загрузка переводов;

сохранение языка в localStorage;

переключатель языков;

перевод по data-i18n.

Исправлено:
перевод больше не ломает href-ссылки (раньше было).

2.3. Десктопная выпадающая панель (header-panel)

При наведении на header.

Несколько колонок с ссылками.

На мобильных скрыта, работает бургер.

2.4. Главная страница — почти полностью обновлена

📌 Важные изменения:

Видео-блок перенесён внутрь hero, теперь сверху текст + сразу видео.

Убрана старая секция how-it-works.

Добавлены новые секции:

Why HealthyBite works

Week with HealthyBite

AI Chat Demo

Добавлен footer.

Всё адаптивно.

2.5. Видео-слайдер (главный блок)

Работает на:

автосмене 8 mp4 файлов;

белом переходе (overlay);

обработке ошибок загрузки;

корректном автозапуске.

2.6. About-страница — полностью исправлена

Исправлены якоря (#how-it-works, #mission, #team).

Секции переработаны:

Hero

How Healthy Bite works

Story/Mission

Principles

Audience

Team

CTA

2.7. Удалён дублирующий “how-it-works” с главной
3. Текущее состояние (важно перед продолжением)
3.1. Главная страница — визуальная проблема

Есть лишнее пустое пространство внизу страниц.
Причина: в одном из CSS-файлов (layout/components/home) есть лишнее правило, которое мы пока не нашли.

Главное:

Это не проблема сервера.

Это не HTML.

Это глобальный CSS (скорее всего из layout.css).

Мы планируем:
📌 провести локализацию бага, почистить CSS и вернуть обновлённый, аккуратный home.css и layout.css.

3.2. Сервер работает корректно

Чтобы запустить сайт:

cd frontend
python -m http.server 8000


И открывать:

http://localhost:8000/index.html
http://localhost:8000/about.html


Все 304 ответы — норм (кеш браузера).

4. Структура проекта (актуальная)
frontend/
 ├── index.html
 ├── about.html
 ├── ai.html
 ├── blog.html
 ├── contact.html
 ├── consultation.html
 ├── css/
 │    ├── base.css
 │    ├── layout.css
 │    ├── components.css
 │    └── pages/
 │         ├── home.css
 │         └── about.css
 ├── js/
 │    ├── main.js
 │    ├── nav.js
 │    └── lang.js
 ├── assets/
 │    ├── videos/
 │    ├── images/
 │    └── icons/
 └── lang/
      ├── en.json
      ├── ru.json
      └── am.json

5. Что осталось доделать
🔥 Главная проблема №1

❗ Исправить лишнее пустое пространство внизу страницы.
Источник: один из CSS файлов (скорее layout.css или components.css).

🔥 Доработка дизайна главной

Добавить декоративные разделители (волны / диагонали).

Сделать секции плотнее.

Добавить мягкие фоны и паттерны.

🔥 Контент для других страниц

Заполнить AI, Blog, Contact, Consultation.

Добавить формы (при необходимости — подключить backend).

🔥 SEO и OpenGraph

Добавить meta description.

Добавить OG картинку.

Добавить favicon.

6. Как использовать этот Summary в новом чате

Если ты откроешь новый чат и напишешь:

«Привет, продолжим HealthyBite, вот мой summary»
и вставишь этот текст —
я продолжу:

с твоими файлами,

с твоей структурой,

с твоей логикой,

с пониманием всех текущих багов и задач.

Без повторных вопросов, без пересборки контекста.