# Cleanup Notes

- Removed `run.py` (empty placeholder; not referenced by any script or README after update).
- Removed `frontend/data/` (contained empty `ai-data.json` and `content.json`; not referenced in code).
- Removed `backend/__pycache__/seed_blog.cpython-310.pyc` (generated artifact).
